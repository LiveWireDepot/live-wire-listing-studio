import {categoryConditions,requireLeafCategory} from "../../../../lib/ebay";
export const runtime="edge";
export async function GET(request:Request){try{const categoryId=String(new URL(request.url).searchParams.get("categoryId")||"").trim();if(!categoryId)return Response.json({error:"Choose a category first."},{status:400});await requireLeafCategory(request,categoryId);const conditions=await categoryConditions(request,categoryId);if(!conditions.length)return Response.json({error:"eBay returned no supported item conditions for this category."},{status:400});return Response.json({categoryId,conditions},{headers:{"cache-control":"no-store"}})}catch(error){return Response.json({error:error instanceof Error?error.message:"Unable to retrieve eBay item conditions."},{status:400,headers:{"cache-control":"no-store"}})}}