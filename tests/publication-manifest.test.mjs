import test from "node:test";import assert from "node:assert/strict";import {buildPublicationManifest,manifestDifferences,manifestHash} from "../lib/publication-manifest.ts";
function input(){return{itemId:"item-2",environment:"production",fees:{feeSummaries:[{fees:[]}]},preflight:{ready:true,offerId:"offer-2",offer:{offerId:"offer-2",sku:"LW-2",marketplaceId:"EBAY_US",format:"FIXED_PRICE",availableQuantity:1,categoryId:"934",listingDescription:"Body",merchantLocationKey:"L1",pricingSummary:{price:{currency:"USD",value:"19.99"}},listingPolicies:{paymentPolicyId:"P",fulfillmentPolicyId:"F",returnPolicyId:"R",bestOfferTerms:{bestOfferEnabled:true}}},inventoryItem:{condition:"USED_GOOD",product:{title:"Title",description:"Body",imageUrls:["one","two"],aspects:{Brand:["RCA"]}},packageWeightAndSize:{weight:{value:1,unit:"POUND"},dimensions:{length:10,width:8,height:1,unit:"INCH"}}}}}}
test("approved manifest retains ordered photos and exact offer fields",async()=>{const manifest=buildPublicationManifest(input());assert.deepEqual(manifest.photos,["one","two"]);assert.equal(manifest.bestOffer.bestOfferEnabled,true);assert.equal(manifest.price.value,"19.99")});
test("changed price is detected before publication",async()=>{const approved=buildPublicationManifest(input()),changed=structuredClone(approved);changed.price.value="24.99";assert.deepEqual(manifestDifferences(approved,changed).map(x=>x.field),["price"]);assert.notEqual(await manifestHash(approved),await manifestHash(changed))});
